let navLinks = document.querySelectorAll("nav ul li a");
let listelements = document.querySelectorAll("nav ul li")

navLinks.forEach(link => {
    link.addEventListener("click", eve => {
        listelements.forEach(li => {

        })
    })
})

